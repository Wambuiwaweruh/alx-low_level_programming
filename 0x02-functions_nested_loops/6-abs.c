#include "main.h"
/**
 * _abs - to print the absolute value of an integer
 * @n: test case number
 * Return: the abs of the int
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

